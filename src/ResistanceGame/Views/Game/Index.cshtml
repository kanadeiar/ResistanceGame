@model GameWebModel
@{
    ViewBag.Title = "Игра";
}

<h4>@ViewBag.Title</h4>

<p>Ваше имя: <strong>@Model.Current.Name</strong></p>

<div id="role">
    <partial name="Partial/RolePartial"/>
</div>

<div class="mt-3"
     hx-get hx-action="Index"
     hx-route-id="@Model.Id"
     hx-trigger="load, every 1s">
    <partial name="Partial/SelectTeamPartial" model="Model"/>
</div>

